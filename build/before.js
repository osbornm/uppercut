(function(window, undefined){
    var $ = this.jQuery,
        ko = this.ko,
        console = this.console;

     if(typeof ko !== 'object'){
        throw 'must include knockout.js before uppercut';
     }
